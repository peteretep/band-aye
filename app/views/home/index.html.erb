<div class="row">
  <div class="col-sm">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="navbar-text"> Gigs You've Signed Up To </div>
  </nav>
  <table class="table">
  <% @gigs_signed_up_to.each do |gig| %>
      <tr><td><%= link_to gig.title, gig %></a></td></tr>
  <% end %>
  </table>
  </div>

  <div class="col-sm">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="navbar-text"> Upcoming Gigs </div>
  </nav>

  <table class="table">
  <% @gigs.each do |gig| %>
      <tr><td><%= link_to gig.title, gig %></a></td></tr>
  <% end %>
  </table>
  </div>
</div>